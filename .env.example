PORT=5000
NODE_ENV=development

# Database configuration (Docker Postgres or Supabase)
DB_HOST=localhost
DB_PORT=5433
DB_NAME=crm_db
DB_USER=crm_user
DB_PASSWORD=crm_password

# JWT configuration
JWT_SECRET=reemplazar_con_un_hash_seguro_en_produccion
JWT_EXPIRES_IN=7d
